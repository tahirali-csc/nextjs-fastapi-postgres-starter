services:
  database:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "postgres"
    restart: always
    volumes:
      - /Users/tahir/workspace/rnd-projects/nextjs-fastapi-postgres-starter/data:/var/lib/postgresql/data

volumes:
  postgres_data: